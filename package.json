{
  "name": "@uozi-admin/root",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "run-p watch:* dev:playground",
    "watch:shared-config": "pnpm -F shared-config watch",
    "watch:request": "pnpm -F request watch",
    "watch:layout": "pnpm -F layout-antdv watch",
    "watch:curd": "pnpm -F curd watch",
    "dev:playground": "pnpm -F playground dev",
    "build": "run-s build:*",
    "build:shared-config": "pnpm -F shared-config build",
    "build:request": "pnpm -F request build",
    "build:layout": "pnpm -F layout-antdv build",
    "build:curd": "pnpm -F curd build",
    "build:create-uozi-admin": "pnpm -F create-uozi-admin build",
    "docs:dev": "pnpm -F docs dev",
    "docs:build": "pnpm -F docs build",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "dependencies": {
    "vue": "^3.5.12"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^3.12.1",
    "@changesets/changelog-github": "^0.5.0",
    "@changesets/cli": "^2.27.9",
    "@types/lodash-es": "^4.17.12",
    "npm-run-all2": "^7.0.2",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "unocss": "^0.65.4",
    "unocss-preset-chinese": "^0.3.3",
    "unocss-preset-ease": "^0.0.4",
    "vite": "^6.0.7",
    "vite-plugin-dts": "^4.5.0",
    "vue-tsc": "^2.2.0"
  }
}
